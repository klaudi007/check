
jwt:
  header: Authorization
  secret: my-very-secret-key-by-Musa-Aliyev
logging:
  level:
    org.springframework.security: PRODUCTION
server:
  port: 8888
spring:
  resources:
    chain:
      enabled: true
management:
  security:
    enabled: true # set to false to disable 'default' Spring Boot security